- ### 主要结构
	- 是链表与数组的结合
	- 数组为主链，每一个数组元素是单向链表
	- [[draws/2023-07-07-09-10-07.excalidraw]]
	- 作用在于结合二者优点
		- 数组查找，链表存储
		- 实现Hash表后，数组相当于字典拼音或笔划，在对应的数组下标中查找想要的值（通过Hash算法实现）
	- 实现 **散列分布均匀**
		- 要求hashCode方法重写，让每次调用返回合适的hash值，将同类元素拥有相同的数组索引
		- 均匀意为
			- <font size = 2 >不能太分散</font>
			- 不能太集中
-